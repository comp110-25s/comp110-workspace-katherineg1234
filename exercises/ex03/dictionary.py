"""The purpose of this exercise is to get some practice with some dictionary functions."""

__author__: str = "730707069" 

def invert(input_dict: dict[str, str]) -> dict[str, str]:
    """Invert the keys and values of a given dictionary and return the inverted dictionary. Raise a KeyError if there is more than one of the same key when trying to invert the dictionary"""

def count (input_list: list[str]) -> dict[str, int]:
    """Given a list, return a dictionary of the counts of each of the items in the input list."""

def favorite_color(input_favcolor: dict[str, str]) -> str:
    """GIven a dictionary of names and favorite colors, return a string which is the color that appears most frequently. If there is a tie, return the first color encountered."""

def bin_len()